# ─── GrowthOS AI — Desktop / Local Development Requirements ─────────────────
# Full requirements for running the desktop app + Telegram bot locally.
# NOT for Railway/server deployment.
# Usage: pip install -r requirements-desktop.txt
# ─────────────────────────────────────────────────────────────────────────────

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# HTTP clients
httpx>=0.27.0
aiohttp>=3.9.0
requests>=2.31.0

# AI
openai>=1.30.0

# Database (full supabase SDK for local use)
supabase>=2.0.0

# Desktop GUI
PyQt5>=5.15.0
PyQtWebEngine>=5.15.0

# Telegram bot
aiogram>=3.5.0

# Utilities
python-dotenv>=1.0.0
python-dateutil>=2.9.0
