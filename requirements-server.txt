# ─── GrowthOS AI — Server Requirements (Railway / VPS) ───────────────────────
# This file is used by Railway. It EXCLUDES:
#   - PyQt5     (desktop GUI — not needed on server)
#   - aiogram   (Telegram bot — deploy separately if needed)
#   - supabase  (full package has pyiceberg build failure on Railway)
#
# Supabase DB access is provided via `postgrest` (lightweight REST client).
# ─────────────────────────────────────────────────────────────────────────────

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# HTTP clients
httpx>=0.27.0
aiohttp>=3.9.0
requests>=2.31.0

# AI
openai>=1.30.0

# Database (Supabase via PostgREST — avoids pyiceberg build issue)
postgrest>=2.0.0
gotrue>=2.0.0

# Utilities
python-dotenv>=1.0.0
python-dateutil>=2.9.0
